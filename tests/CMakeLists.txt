# Placeholder for tests - real tests will be added in later stories.
# Use a CMake built-in command so the bootstrap story has a stable passing test.

add_test(NAME placeholder COMMAND ${CMAKE_COMMAND} -E echo "All placeholder tests passed.")
