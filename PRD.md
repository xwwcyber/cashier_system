# C++ 收银系统需求文档（PRD）

## 1. 文档说明

- **项目名称**：C++ 收银系统
- **文档版本**：v1.0
- **文档类型**：需求文档 / 产品需求说明
- **适用场景**：课程设计、练手项目、小型门店收银流程模拟

> 说明：由于当前未指定界面与存储细节，本文档采用一套适合教学与中小型项目实现的默认方案：**C++ 控制台程序 + CMake 构建 + 文件持久化（CSV/JSON）**。后续如需改成 Qt 图形界面或 SQLite 数据库，可在此文档基础上扩展。

---

## 2. 项目背景

传统收银工作需要完成商品录入、购物结算、找零、库存扣减、订单记录和销售统计等流程。为帮助学习 C++ 面向对象设计、文件读写、模块化开发与基础业务建模，计划开发一套收银系统，用于模拟真实门店的核心收银业务。

该系统面向小型商店、超市、便利店或课程项目场景，支持商品管理、购物车、折扣优惠、库存管理、员工权限管理以及销售报表统计等功能。

---

## 3. 项目目标

1. 使用 C++ 完成一个可运行的收银系统。
2. 支持完整的基础收银流程：登录、选购、结账、找零、保存订单。
3. 支持后台管理能力：商品管理、库存管理、员工管理、销售统计。
4. 采用文件方式持久化数据，保证程序重启后基础数据不丢失。
5. 代码结构清晰，便于后续扩展为图形界面版本或数据库版本。

---

## 4. 项目范围

### 4.1 范围内功能

- 员工登录与角色权限控制
- 商品增删改查
- 商品库存维护
- 购物车管理
- 结账与找零计算
- 折扣 / 优惠券处理
- 订单记录保存
- 销售报表统计
- 文件持久化

### 4.2 暂不包含

- 网络联机收银
- 多门店数据同步
- 支付宝 / 微信 / 银行卡真实支付对接
- 发票打印、扫码枪、钱箱等硬件接入
- Web 管理后台

---

## 5. 用户角色

### 5.1 管理员（Admin）

权限包括：
- 商品管理
- 库存管理
- 员工管理
- 优惠券管理
- 查看订单与销售报表
- 执行基础收银操作

### 5.2 收银员（Cashier）

权限包括：
- 登录系统
- 浏览商品
- 添加商品到购物车
- 结账与找零
- 查询个人当班订单

限制：
- 不允许修改商品基础信息
- 不允许管理员工
- 不允许查看完整经营报表

---

## 6. 业务流程

### 6.1 收银主流程

1. 员工登录系统。
2. 系统根据角色展示对应菜单。
3. 收银员选择商品并加入购物车。
4. 系统计算购物车总价。
5. 可选择输入优惠券或折扣信息。
6. 系统计算优惠后应付金额。
7. 收银员输入实收金额。
8. 系统计算找零并完成结账。
9. 系统生成订单记录并扣减库存。
10. 数据写入文件持久化存储。

### 6.2 后台管理流程

1. 管理员登录系统。
2. 进入商品、库存、员工、优惠券或报表菜单。
3. 执行新增、修改、删除、查询等管理操作。
4. 变更结果实时保存到数据文件。

---

## 7. 功能需求

## 7.1 登录与权限管理

### 功能描述
系统支持员工账号登录，并根据角色展示不同菜单与权限。

### 需求明细
- 员工使用工号/账号 + 密码登录。
- 系统校验账号密码是否正确。
- 登录成功后保存当前会话信息。
- 根据角色显示不同功能菜单。
- 支持退出登录。
- 管理员可新增员工、删除员工、重置密码。

### 规则说明
- 普通收银员不能访问商品管理、员工管理和完整销售报表。
- 密码不能明文展示。
- 连续输错密码达到指定次数后，系统提示返回或退出。

---

## 7.2 商品管理

### 功能描述
管理员可对商品信息进行维护，包括新增、删除、修改、查询。

### 商品字段
- 商品编号（唯一）
- 商品名称
- 商品分类
- 商品单价
- 商品库存
- 商品状态（上架 / 下架）

### 需求明细
- 支持新增商品。
- 支持按商品编号删除商品。
- 支持修改商品名称、价格、分类、库存、状态。
- 支持查看全部商品列表。
- 支持按名称模糊查询。
- 支持按分类筛选商品。

### 规则说明
- 商品编号必须唯一。
- 商品价格必须大于 0。
- 商品库存不能小于 0。
- 已下架商品不能加入购物车。

---

## 7.3 购物车管理

### 功能描述
收银员将商品加入购物车，支持查看、修改数量、删除商品。

### 需求明细
- 按商品编号添加商品到购物车。
- 输入购买数量。
- 购物车中显示商品名称、单价、数量、小计。
- 支持修改购物车中商品数量。
- 支持从购物车移除商品。
- 实时计算购物车总金额。

### 规则说明
- 购买数量不能超过当前库存。
- 数量必须为正整数。
- 如果库存不足，系统应提示并拒绝加入。

---

## 7.4 结账与找零

### 功能描述
系统根据购物车内容计算应付金额，并支持实付金额输入与找零计算。

### 需求明细
- 计算商品原价总额。
- 应用折扣或优惠券后计算最终应付金额。
- 输入顾客实付金额。
- 系统自动计算找零金额。
- 结账成功后生成订单编号与订单记录。

### 规则说明
- 实付金额必须大于等于应付金额。
- 找零 = 实付金额 - 应付金额。
- 订单完成后不可再次修改。
- 订单完成后系统自动更新库存。

---

## 7.5 折扣与优惠券

### 功能描述
系统支持基础折扣与优惠券功能，用于结账优惠处理。

### 需求明细
- 支持百分比折扣，例如 9 折、8.5 折。
- 支持固定金额优惠券，例如满 100 减 10。
- 支持输入优惠券编号进行核销。
- 管理员可新增和管理优惠券。
- 优惠券可配置最低消费门槛。

### 优惠券字段
- 优惠券编号
- 优惠类型（百分比 / 固定减免）
- 优惠值
- 最低消费金额
- 是否已使用
- 有效状态

### 规则说明
- 同一订单默认只允许使用一张优惠券。
- 优惠后金额不得小于 0。
- 已使用或无效优惠券不能再次使用。

---

## 7.6 库存管理

### 功能描述
管理员查看商品库存，并进行补货或库存预警处理。

### 需求明细
- 查看当前库存列表。
- 按库存数量排序。
- 支持补货操作。
- 支持设置低库存阈值。
- 当库存低于阈值时显示预警。

### 规则说明
- 补货数量必须为正整数。
- 库存变更后应立即保存。
- 库存为 0 的商品不可出售。

---

## 7.7 订单管理

### 功能描述
系统保存每一笔结账订单，供后续查询与统计。

### 订单字段
- 订单编号
- 下单时间
- 收银员编号
- 商品明细列表
- 原价总额
- 优惠金额
- 实付金额
- 实收金额
- 找零金额

### 需求明细
- 每次结账成功后生成订单。
- 支持查看订单列表。
- 支持按日期查询订单。
- 支持按订单编号查询订单详情。

---

## 7.8 销售报表统计

### 功能描述
管理员可查看系统销售情况，用于经营分析。

### 需求明细
- 查看今日销售额。
- 查看累计销售额。
- 查看订单总数。
- 统计销量 Top N 商品。
- 按商品分类统计销售额。
- 按日期范围统计销售数据。

### 输出建议
- 控制台表格化展示报表。
- 支持导出为文本或 CSV（可作为扩展需求）。

---

## 7.9 数据持久化

### 功能描述
系统使用文件保存核心业务数据，保证程序关闭后可恢复。

### 持久化对象
- 商品数据
- 员工数据
- 优惠券数据
- 订单数据
- 配置数据

### 存储建议
- `data/products.csv` 或 `data/products.json`
- `data/employees.csv` 或 `data/employees.json`
- `data/coupons.csv` 或 `data/coupons.json`
- `data/orders.csv` 或 `data/orders.json`
- `data/config.txt` 或 `data/config.json`

### 规则说明
- 首次启动时若文件不存在，应自动初始化示例数据。
- 文件格式需统一，避免同类数据混用多种格式。
- 写入失败时应提示错误，避免数据静默丢失。

---

## 8. 非功能需求

## 8.1 易用性
- 控制台菜单层级清晰。
- 重要操作前给出提示。
- 错误输入后不崩溃，允许重新输入。
- 关键结果（总价、优惠、找零、库存不足）需明显展示。

## 8.2 稳定性
- 系统对非法输入具备基本容错能力。
- 文件不存在、文件格式错误时有异常提示。
- 关键流程（登录、结账、保存）不能因单次输入错误直接退出。

## 8.3 安全性
- 不在代码中硬编码敏感信息。
- 员工密码至少做基础哈希处理，不以明文存储。
- 普通收银员不可越权访问管理员功能。
- 对输入内容做格式校验，避免非法金额和非法数量。

## 8.4 可维护性
- 采用模块化设计，业务模块分离。
- 数据模型、业务逻辑、文件读写、界面交互分别组织。
- 便于后续扩展 GUI、数据库或网络版本。

## 8.5 性能
- 适用于小型门店或课程项目规模。
- 支持至少数百个商品、数千笔订单的本地管理。
- 常见操作（查询、结账、保存）应在可接受范围内完成。

---

## 9. 主要数据对象建议

## 9.1 商品 Product
- id
- name
- category
- price
- stock
- status

## 9.2 员工 Employee
- id
- name
- passwordHash
- role

## 9.3 购物项 CartItem
- productId
- productName
- unitPrice
- quantity
- subtotal

## 9.4 优惠券 Coupon
- code
- type
- value
- minAmount
- used
- enabled

## 9.5 订单 Order
- orderId
- timestamp
- cashierId
- items
- originTotal
- discountAmount
- payableAmount
- paidAmount
- changeAmount

---

## 10. 技术实现建议

- **开发语言**：C++17 或以上
- **构建工具**：CMake
- **运行方式**：控制台程序
- **数据存储**：CSV 或 JSON 文件
- **推荐目录结构**：

```text
cashier_system/
├─ CMakeLists.txt
├─ include/
│  ├─ models/
│  ├─ services/
│  ├─ repositories/
│  └─ utils/
├─ src/
│  ├─ main.cpp
│  ├─ ui/
│  ├─ services/
│  └─ repositories/
├─ data/
└─ tests/
```

---

## 11. 验收标准

满足以下条件视为项目达到基础验收要求：

1. 系统可通过 CMake 成功构建并运行。
2. 支持员工登录，并根据角色显示不同菜单。
3. 管理员可完成商品增删改查。
4. 收银员可完成商品选择、购物车管理、结账和找零。
5. 系统支持至少一种优惠方式（折扣或优惠券）。
6. 订单完成后库存自动扣减。
7. 商品、员工、订单等数据可保存到文件并在重启后恢复。
8. 管理员可查看基础销售报表。
9. 程序对常见错误输入不会崩溃。

---

## 12. 迭代建议

### 第一阶段（MVP）
- 登录
- 商品管理
- 购物车
- 结账找零
- 文件保存

### 第二阶段
- 优惠券
- 库存预警
- 报表统计
- 员工管理

### 第三阶段
- 图形界面（Qt）
- SQLite 数据库存储
- 小票打印
- 多用户并发支持

---

## 13. 风险与注意事项

1. 控制台程序在界面交互体验上有限，需要通过清晰菜单弥补。
2. 若使用 CSV 存储复杂订单明细，格式设计需提前统一。
3. 若后续扩展为数据库版本，建议保持数据模型与存储层解耦。
4. 密码、金额、库存等字段必须严格校验，避免逻辑错误。

---

## 14. 结论

本项目适合作为一个完整的 C++ 综合练习项目，覆盖面向对象设计、文件操作、模块拆分、权限控制、业务流程建模等核心能力。通过该系统可以较完整地模拟小型门店收银业务，并为后续升级为 GUI 或数据库系统打下基础。
